{% extends '_layouts/user_base.html.twig' %}

{% block title %}Catalogue des Livres{% endblock %}

{% block body %}
<div class="space-y-6">
    <h1 class="text-3xl font-bold text-gray-900">Catalogue des Livres</h1>
    
    {# Formulaire de filtrage #}
    <div class="bg-white shadow rounded-lg p-6">
        <form method="get" action="{{ path('user_livres') }}" class="flex flex-wrap gap-4 items-end">
            <div class="flex-1 min-w-[200px]">
                <label for="bibliotheque" class="block text-sm font-medium text-gray-700 mb-2">Bibliothèque</label>
                <select name="bibliotheque" id="bibliotheque" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
                    <option value="">Toutes les bibliothèques</option>
                    {% for bibliotheque in bibliotheques %}
                        <option value="{{ bibliotheque.id }}" {% if selectedBibliotheque == bibliotheque.id %}selected{% endif %}>
                            {{ bibliotheque.nom }}
                        </option>
                    {% endfor %}
                </select>
            </div>
            <div class="flex-1 min-w-[200px]">
                <label for="categorie" class="block text-sm font-medium text-gray-700 mb-2">Catégorie</label>
                <select name="categorie" id="categorie" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
                    <option value="">Toutes les catégories</option>
                    {% for categorie in categories %}
                        <option value="{{ categorie.id }}" {% if selectedCategorie == categorie.id %}selected{% endif %}>
                            {{ categorie.designation }}
                        </option>
                    {% endfor %}
                </select>
            </div>
            <div class="flex gap-2">
                <button type="submit" class="px-6 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition">
                    Filtrer
                </button>
                <a href="{{ path('user_livres') }}" class="px-6 py-2 bg-gray-300 text-gray-700 rounded-lg hover:bg-gray-400 transition">
                    Réinitialiser
                </a>
            </div>
        </form>
    </div>
    
    {% if livres|length == 0 %}
        <div class="bg-white shadow rounded-lg p-12 text-center">
            <p class="text-gray-500 text-lg">Aucun livre ne correspond à vos critères de recherche.</p>
            <a href="{{ path('user_livres') }}" class="mt-4 inline-block text-indigo-600 hover:text-indigo-800">Voir tous les livres</a>
        </div>
    {% else %}
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for livre in livres %}
        <a href="{{ path('user_livre_show', {id: livre.id}) }}" class="bg-white shadow rounded-lg overflow-hidden hover:shadow-lg transition cursor-pointer block">
            <div class="aspect-w-16 aspect-h-22 bg-gray-200 overflow-hidden">
                {% if livre.imageName %}
                    <img src="{{ vich_uploader_asset(livre, 'imageFile') }}" alt="{{ livre.titre }}" class="w-full h-64 object-cover">
                {% else %}
                    <div class="w-full h-64 bg-gray-300 flex items-center justify-center">
                        <svg class="w-24 h-24 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
                        </svg>
                    </div>
                {% endif %}
            </div>
            <div class="p-6">
                <h3 class="text-xl font-semibold text-gray-900 mb-2">{{ livre.titre }}</h3>
                <div class="space-y-2 text-sm text-gray-600 mb-4">
                    {% if livre.auteur %}
                        <p><span class="font-medium">Auteur:</span> {{ livre.auteur.nom }} {{ livre.auteur.prenom }}</p>
                    {% endif %}
                    {% if livre.categorie %}
                        <p><span class="font-medium">Catégorie:</span> {{ livre.categorie.designation }}</p>
                    {% endif %}
                    {% if livre.editeur %}
                        <p><span class="font-medium">Éditeur:</span> {{ livre.editeur.nom }}</p>
                    {% endif %}
                    {% if livre.bibliotheque %}
                        <p><span class="font-medium">Bibliothèque:</span> {{ livre.bibliotheque.nom }}</p>
                    {% endif %}
                    <p><span class="font-medium">Stock:</span> {{ livre.qte }} exemplaire(s)</p>
                </div>
                <div class="flex justify-between items-center mt-4">
                    <span class="text-indigo-600 hover:text-indigo-800">Voir détails →</span>
                    {% if livre.qte > 0 %}
                        <span class="bg-indigo-600 text-white px-4 py-2 rounded-lg text-sm">
                            Disponible
                        </span>
                    {% else %}
                        <span class="text-gray-400 text-sm">Rupture de stock</span>
                    {% endif %}
                </div>
            </div>
        </a>
        {% endfor %}
    </div>
    {% endif %}
</div>
{% endblock %}


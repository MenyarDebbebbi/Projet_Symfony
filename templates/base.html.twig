<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Système de Gestion{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <script src="https://cdn.tailwindcss.com"></script>
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="bg-gray-50 min-h-screen">
        <nav class="bg-indigo-600 text-white shadow-lg">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex items-center space-x-8">
                        <a href="{{ path('app_accueil') }}" class="text-xl font-bold flex items-center hover:text-indigo-200 transition">
                            <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                            </svg>
                            Gestion
                        </a>
                        <div class="hidden md:flex items-center space-x-1">
                            <a href="{{ path('app_livre_index') }}" class="hover:bg-indigo-700 px-3 py-2 rounded-md transition text-sm">
                                Livres
                            </a>
                            <a href="{{ path('app_auteur_index') }}" class="hover:bg-indigo-700 px-3 py-2 rounded-md transition text-sm">
                                Auteurs
                            </a>
                            <a href="{{ path('app_categorie_index') }}" class="hover:bg-indigo-700 px-3 py-2 rounded-md transition text-sm">
                                Catégories
                            </a>
                            <a href="{{ path('app_editeur_index') }}" class="hover:bg-indigo-700 px-3 py-2 rounded-md transition text-sm">
                                Éditeurs
                            </a>
                            <a href="{{ path('app_ouvrier_index') }}" class="hover:bg-indigo-700 px-3 py-2 rounded-md transition text-sm">
                                Ouvriers
                            </a>
                        </div>
                    </div>
                    <div class="flex items-center">
                        <a href="{{ path('app_accueil') }}" class="bg-indigo-500 hover:bg-indigo-400 px-4 py-2 rounded-md transition font-medium text-sm flex items-center">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
                            </svg>
                            Dashboard
                        </a>
                    </div>
                </div>
            </div>
        </nav>
        
        <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
